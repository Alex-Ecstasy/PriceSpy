@model CardTemplate

<div class="card" style="width: 280px;">
    <img src="@Model.Picture" style="object-fit:cover; width: 100%; height:200px; fill:aqua">
    <div class="card-body">
        <h6 class="card-text">@Model.CatNumber</h6>
        @if (@Model.IsAvailable == true)
        {
            <a class="btn btn-primary" href="@(Model.UrlPrefix)@(Model.CardUrl)">@Model.Price</a>
            <h6 class="card-text">@Model.Status</h6>
        }
        else
        {
            <a class="btn btn-outline-danger" style="padding: 0,375rem 4rem; display:table;" href="@(Model.UrlPrefix)@(Model.CardUrl)">@Model.Price</a>
            <h7 class="card-text">@Model.Status</h7>
        }
        <p class="card-title">@Model.Name</p>
    </div>
</div>